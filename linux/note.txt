


linux 的基本 认识




配置网络连接时,有三种形式,
1,桥接模式: linux可以和其它的系统通信,但是可能造成ip地址冲突
2. NAT  :  网络地址转换方式,不会造成冲突,会有三个ip地址,一个主机的ip,两个ip地址是用来同个机器上两个操作系统通信的
3,主机模式:你的linux是一个独立的主机,不能访问外网

进入右键 open terminal
可以到 edit中自定义设置
为了 能windows和linux两系统能够 copy and paste
可以安装 vmware-tools
先到vmware中选择 vmware-tools
然后在 terminal 中 cd到对应的目录
执行解压操作：  tar -vxzf 对应的文件
cd 进入解压文件中 
执行 ./vmware-install.pl
没有 super user 可以 使用 su -  root
可以让Windows和linux共享同文件
vmware中找到虚拟机选项 --> 设置 --> 选项 --> 共享文件  --> 开启 -->指定文件
在 centos系统下，找mnt下hgfs下 可以找到 对应的共享文件


在linux世界里， 一切皆是文件
在linux操作系统中
 /     表示 根目录
根目录下
/bin ：   是binary的缩写，这个目录放着经常使用的命令
/sbin：  super user超级用户，这里存放的是系统管理员的系统管理程序
/home： 存放普通用户的主目录，在linux中每个用户都有一个自己的目录，一般该目录名是以用户的账号命名的
/root:     该目录为系统管理员，也称作超级权限者的用户主目录
/lib：    系统开机所需要最基本的动态连接共享库,其作用类似于windows里的DL文件。几乎所有的应用程序都需费用到这些共享库.
/ost-found：   这个目录一般情况下是空的,当统非法关机后,这里就存放了一些文件。
/etc：     所有的系统管理所需要的配置文件和子目录my.conf
/usr：    这是一个非常重要的目录,用户的很多应用程序和文件都放在这个目录下,类似与windows下的programfiles目录
/boot:    重点存放的是启动Linux时使用的一些核心文件,包括一些连接文件以及镜像文件


/////////////////////下面三个最好不要动
/proc:      这个目录是一个虚拟的目录,它是系统内存的映射,访问这个目录来获取系统信息. 
/srv:         service缩写, 该目录存放一些服务启动之后需要提取的数据。
/sys:       这是linux2.6内核的一个很大的变化。该目录下安装了2.6内核中新出现的一个文件系统fs.
///////////////////////////////////////////////////////////////

/tmp:      这个目录是用来存放一些临时文件的。
/dev：       类似于windows的设备管理器,把所有的硬件用文件的形式存储。
/media：   linux系统会自动识别一些设备,例如U盘、光驱等等,当识别后, linux会把识别的设备挂就到这个目录下。
/mnt：       系统提供该目录是为了让用户时挂载别的文件系统的,我们可以将外部的存储挂载在/mnt/上,然后进入该目录就可以查看里的内容了.
/opt：       这是给主机额外安装软件所摆放的目录。如安装ORACLE 据库就可放到该目录下。默认为空。
/usr/local：          这是另一个给主机额外安装软件所安装的目录。一般是通过编译源码方式安装的程序
/var:         这个目录中存放着在不断扩充着的东西,习惯将经常被修改的目录放在这个目录下。包括各种日志文件。
/selinux :            (security-enhanced linux) , SELinux是一种安全子系统,它能控制程序只能访问特定文件。


注意， 这些目录都是规划好的，不要随意的改变
linux 只有一个根目录
linux 是以文件的形式管理我们的设备的，因此一切都是文件
linux 下各个文件目录下存放什么内容，都是安排好的。


cd /xx/xxx  改变目录路径， cd  change directory



为什么要远程登录linux系统?

Linux 一般作为服务器使用，而服务器一般放在机房，你不可能在机房操作你的 Linux 服务器。
这时我们就需要远程登录到Linux服务器来管理维护系统。

下载 Xshell软件 查linux的ip地址用于xshell远程登录连接
linux查询ip命令
ifconfig
下载Xftp, 远程上传下载文件

vi 文本编辑器
vim vi的增强版

vim 有三种模式：
正常模式： 
插入模式/编辑模式：
命令行模式：

vim xx.xx（文件名.扩展名）:  进入   正常模式   -->
 i,I,O,o等键        进入   插入模式/编辑模式 -->
esc键  退出插入模式 -->
： 进入命令模式
:wq   :q  :q!
:wq 写 然后退出
:q 退出
:q! 强制退出

光标行  esc退出编辑模式  按命令  yy（copy）  然后按 p (粘贴 )
esc退出后  按 数字(123...) 表示要复制多少行   然后 yy 复制
dd 删除     按 数字(123...) 表示要复制多少行   然后 dd 删除

命令模式   /关键字  n 搜索
设置行号    :set nu    取消行号      :set nonu

正常模式  按 G 键 到末尾     按 gg 到头

正常模式 按u上一步(撤销)

先显示行数  在正常模式   输入 对应的行数（数字） 然后  shift键+g跳转到对应的行数 
先显示行数  在正常模式   输入 对应的行数（数字） 然后  space键跳转到对应的列数 

关机和重启命令
shutdown
	shutdown -h now          立即关机
	shutdown -h 1 	      一分钟后关机
	shutdown -r now	      立即重启
halt		关机，直接使用
reboot		重启，直接使用
sync		把内存的数据同步到磁盘， 建议关机和重启之前，先执行这个

用户的登录和注销
logout 注销, 图形用户界面下没用
su - root     命令来切换成系统管理员身份

linux系统是一个多用户多任务的操作系统，任何一个要使用系统资源的用户，
都必须首先向系统管理员申请一个账号，然后以这个账号的身份进入系统

linux的用户至少要属于一个组， 每个用户登录linux系统，都会到自己的home目录下
每个用户有自己的home/xx 目录，  用户需要在组里。
当创建用户成功后，会自动的创建和用户同名的home目录，
也可以通过	useradd -d 指定目录   新用户名       ，给新创建的用户指定目录
pwd 查看用户的目录

////////////////////  root用户可以玩的
添加用户
useradd [选项]  用户名

指定/修改密码
passwd  用户名          为指定用户添加密码，没有指定用户名，就是给root设置密码

删除用户
userdel 用户名
1.仅删除用户
userdel 用户名
2. 删除用户 + 他的home目录
userdel -r 用户名
在删除用户时， 不要把人家的home目录也给删除了


查询用户信息
id 用户名
uid 表示userid    gid表示groupid

切换用户
su -  需要切换的用户名
exit  返回到原来的用户
root用户切换到普通用户 不需要密码，反之则需要
who am i    命令查看"我"是谁

用户组
类似于角色，系统可以对有共性的多个用户统一的管理

添加组
groupadd  组名
添加用户到指定的组中
useradd -g 组名  用户名
修改用户的组
usermod -g 用户组 用户名
删除用户的组
groupdel 组名

/etc/passwd   文件：   用户user的配置文件，记录用户的各种信息
vim该文件后  每行的含义      
    aili  :      x  :        1001    :       1002  :                    :  /home/aili   :   /bin/bash
用户名：口令：用户标识符：组标识号：注释性描述：主目录         ：登录shell

/etc/shadow  文件：口令（密码）的配置文件，
vim该文件后  每行的含义  
 登录名：加密口令： 最后一次修改时间： 最小时间间隔：最大时间间隔：警告时间：不活动时间：失效时间：标志

/etc/group 文件：组的配置文件，记录linux包含的组的信息
vim该文件后  每行含义
组名：口令：组标识号：组内用户列表


			linux的使用指令
一。指定运行级别
	七个运行级别： 
		0.  关机
		1.  单用户（找回丢失密码）
		2.  多用户状态没有网络服务
		3.  多用户状态有网络服务
		4.  系统未使用保留给用户
		5.  图形界面
		6.  系统重启
系统的运行级别配置文件  /etc/inittab
修改默认的运行级别可改文件中    id:5:initdefault：这一行数字
命令 init [012356]
如   init 3  到多用户状态没有网络服务 runlevel

//////////////////////////////////////////////////
忘了root密码，如何找回root密码，？
进入到单用户模式
然后修改root 的密码
原理：  进入单用户模式root不需要密码就可以登录

步骤：  基于centos7   
centos开机时，默认的一项  按 e 键进入特定界面
按方向键一直到最后面，看到  界面中有
if   xxx   else  xxx  fi 
   就在fi下面有什么  linux16 /vmlinuz.....就这一块
找到   ro  （read only）关键字 改为  rw init=/sysroot/bin/sh
按 Ctrl + x 进行重启进入到单用户模式

看到     :/#   提示符， 输入  chroot /sysroot  命令（其中chroot命令用来切换系统，/sysroot/目录就是原始系统）

要修改密码    键入命令   passwd    修改root的密码
接着  键入命令   touch /.autorelabel   执行这行命令作用是让SELinux生效，如果不执行，密码不会生效。
按Ctrl+D  退出，
执行reboot命令重启后生效
//////////////////////////////////////////////////
单用户模式 来修改默认的运行级别：
systemctl set-default multi-user.target  		#设置成命令模式
init 3 		# 切换到字符模式，有时只使用上面的语句没有效果
exit后，执行reboot


二。 帮助指令
当俺们对某个指令不熟悉时，俺们可以使用Linux的帮助指令
  获取帮助信息
man指令（获得帮助信息）
man [命令或配置文件]

help指令 （获取shell内置命令的帮助信息）
help  命令 


三。 文件目录类
pwd   指令 显示当前工作目录的绝对路径
pwd

ls 指令
ls  [选项] [目录或文件]
常用 选项  ：  -a 显示当前目录所有的文件和目录(包括隐藏文件)	   -l  以列表的方式显示信息

cd 指令  切换到指定的目录
cd [参数]
常用参数：  cd ~ 或者  cd  回到自己的home目录      cd  ..  回上一目录

mkdir 指令  用于创建目录(make directory)
mkdir [选项] 要创建的目录
常用的选项：    -p   创建多级目录

rmdir 指令  删除空目录
rmdir [选项] 要删除的空目录
如果需要删除非空目录，需要使用   rm -rf  要删除的目录

touch 指令  创建空文件
touch 文件名    （可以一次性创建多个）

cp  指令   copy文件到指定目录
cp [选项] source  dst         //  source 表示要copy的文件， dst表示指定的目标目录
常用选项：   -r  递归复制整个文件夹          source此时是文件夹
如果cp时dst中有与source同名的文件，    可以强制覆盖,不提示(使用\)     \cp  source  dst 

rm  指令  移除文件或目录
rm [选项] 要删除的文件
常用的选项：  -r  递归删除整个文件夹   -f  强制删除不提示

mv 指令  移动文件与目录或重命名
mv oldFilename newFilename     //重命名，  当前目录有oldfile将会重命名
mv /temp/movefile  /targetFolder  // 移动文件（第三项是文件）

cat 指令 查看文件的内容
cat [选项] 要查看的文件
常用选项：   -n  显示行号
cat指令只能浏览文件的内容，不能修改，一般回带上 管道指令(|)    | more   这样就可以慢慢显示

more 指令   一个基于vi编辑器的文件过滤器，它以全屏的方式按页显示文本文件的内容
more 要查看的文件
more指令后，有几个内置的快捷键：
	space（空格）  向下翻一页
	enter   向上翻一行
	q     立即退出
	ctrl+F   向下滚动一页
	ctrl+B  向上滚动一页

less  指令  用来分屏查看文件内容，它的功能与more指令类似，但是less支持各种形式终端，
在less显示文件内容时，并不是一次将整个文件加载之后从显示，而是根据显示需要加载内容
less 要查看的文件
less指令后，有几个内置的快捷键：
	space（空格）  向下翻一页
	pagedown   向下翻一页
	pageup  向上翻一页
	/要搜索的内容   向下查找关键字， n：向下   N:向上
	?要搜索的内容   向上查找关键字， n：向上   N:向下
	q     立即退出

>   输出重定向     >> 追加
语法：
ls -l >文件              将显示列表的内容重写到文件，没有文件就创建一个
ls -al >>文件         将显示的列表内容追加的文件首行
cat 文件 >文件      将查看的文件内容重写到文件中
echo "内容" >>文件    将指定的内容追加到文件中          

echo 指令 输出内容到控制台
echo  [选项] [输出的内容]

环境变量 $XXX

head 指令  用于显示文件的开头部分内容，默认情况下head指令显示文件前10行内容
head 文件
head -n 9 文件      （查看开头的9行）

tail 指令  用于显示文件的结尾部分内容，默认情况下head指令显示文件后10行内容
tail 文件
tail -n 6 文件   	（查看结尾的6行）
tail -f 文件	（用的很多，实时跟踪该文件的所有更新, 此模式下按ctrl+c退出）


ln  指令 软链接，符号链接，类似windows下的快捷方式，主要存放链接其他文件的路径
ln -s [原文件或目录] [软链接名]
cd指令使用软链接，到原文件目录
rm -r 删除软链接  不要在删除时加 /


history 指令 查看已经执行过的历史命令，也可以执行历史指令
history 
使用历史中的命令，每个命令都有No（行数）， 使用    !No   执行对应历史命令


四。时间日期类
date  指令  显示当前日期
date   显示当前时间
date  +%Y   显示当前年份
date  +%m   显示当前月份
date  +%d    显示当前天数
date  "+%Y-%m-%d  %H:%M:%S"  显示某年某日某月几时几分几秒
	（Y 表示20xx     y表示xx    m表示月    M表示时）
date  -s  字符串时间         保存日期  例如  "2020-9-18 10:32:45"

cal 指令  查看日历
cal [选项]   不加选项，显示本月的日历
cal 2020 显示整年的日历


五。搜索查找类
find 指令 将从指定目录向下递归遍历其各个子目录，将满足条件的文件或目录（的路径）显示在terminal中
find [搜索范围]  [选项]
常用选项：  -name 文件           通过文件名搜索(文件含扩展名， 可以用通配符 * )
	   -user  用户名        按用户名，找他所属的文件
	   -size  +n(大于n)  -n(小于n)  n(大于n)          按文件大小查找，n为指定的大小， n带单位， c k M G

locate  指令  可以快速定位文件路径
locate指令利用事先建立的系统中所有文件名称及路径的locate数据库实现快速定位给定的文件. 
Locate指令无需遍历整个文件系统,查询速度较快为了保证查询结果的准确度,
管理员必须定期更新locate时刻
·基本语法
 locate 搜索文件
特别说明由于locate指令基于数据库进行查询,
所以第一次运行前,必须使用updatedb指令创建locate数据库.

grep 指令  过滤查找的内容， 
管道符 "|" , 表示将前一个命令的处理结果输出传递给后面的命令处理
grep [选项] 查找内容 源文件
还可以
cat 源文件  |  grep  查找内容
...
常用选项：  -n  显示匹配的行和行号    -i  忽略字母大小写

六。压缩和解压类
gzip 指令   用于压缩文件， 压缩不会保留原来的文件
gzip  文件	         只能将文件压缩为 *.gz格式

gunzip 指令  用于解压文件
gunzip  *.gz（文件）

zip 指令  
zip  用于压缩文件，不会删除原来的文件
zip [选项]  xxx.zip  将要压缩的内容
常用选项：   -r   递归压缩，即压缩目录

unzip 指令
unzip 用于解压文件
unzip [选项]  xxx.zip 
常用选项：   -d  指定解压后文件的存放目录

tar 指令 打包指令，最后打包后的文件是以 .tar.gz 为扩展名，可以打包文件或目录
tar [选项]  xxx.tar.gz  打包的内容
选项：   -c  产生.tar打包文件      -v  显示详细信息    -f 指定压缩后的文件名
-z  打包同时压缩	-x 解压.tar文件
tar -zcvf  	组合使用  注意 f 要在后面
tar -zxvf 	组合使用  注意 f 要在后面
tar -zxvf  xxx.tar.gz  -C  /xx/xx         解压到指定目录（指定目录得存在）


			组管理和权限管理
linux组的介绍
在linux中每个用户必须属于一个组，不能独立于组外，
在Linux中每个文件有所有者，所在组，其他组的概念
 
1. 文件/目录所有者
一般为文件的创建者，谁创建了该文件，就是该文件的所有者
查看 文件的所有者     ls  -ahl      (a   all   h  human   l list    所有人列表)
修改文件的所有者      chown  用户名  文件名   （chown change own）
修改文件的所有者和所在组     chown 用户名:组名 文件名
递归修改某目录下及子目录的所有文件的所有者       chown  -R 用户名  目录
 
2. 文件/目录所在组
当某个用户创建一个文件后，默认这个文件的所在组就是该用户的所在组
查看 文件的所在组     ls  -ahl      (a   all   h  human   l list    所有人列表)
修改文件的所在组      chgrp  组名  文件名   （chgrp change group）
递归修改某目录下及子目录的所有文件的所在组       chgrp -R 组名  目录

3. 除文件的所有者和所在组的用户外，系统的其他用户都是文件的 其他组

linux(文件或目录)权限介绍
ls -ahl后
-           		rw-       		 r--         		  	r--. 			1 	 	guyi 	guyi    	 11 	  Sep  8 22:57  	test.txt
文件的类型	所有者的权限	文件的所在组的用户权限	文件的其他组的用户权限	表示硬链接	所有者	所在组	 文件大小	   最后修改的日期	文件名

- 普通文件  d目录  l表示软链接    c表示字符设备(键盘，鼠标)    b表示块文件，硬盘
rw-    表示文件 的所有者的权限为  rw(可读可写)    如果是 -  表示没有权限
r--      表示文件的所在组的用户权限为   r(只读)    如果是 -  表示没有权限
r--       表示文件的其他组的用户权限为  r(只读)     。。。
1         表示硬链接，如果是目录的话，这就表示目录下有多少的子目录， 目录中有隐藏的  ..  和  . 目录
11       表示文件的大小
Sep  8 22:57     最后修改的日期
test.txt    文件名

rwx作用到文件， r 可读          w可写，不一定就可以删除文件，还得看看它所在目录是不是可写          x可执行
rwx作用到目录， r可读，ls         w可写，目录内删除创建，重命名目录        x可执行，可以进入该目录

rwx可为数字，，，r =4  w=2  x=1 ， rwx=7
文件大小总会多一个字符

修改权限
chmod  指令   可以修改文件或目录的权限
u所有者	g所在组	o其他组	a所有人（ugo的总和）
chmod u=rwx,g=rx,o=r  文件或目录名    （指定的文件或目录的所有者权限为可读可写可执行，所在组权限为可读可执行，其他组权限为只读）等价     chmod  754
chmod  u+w  文件或目录名    （给指定的文件或目录 追加  所有者权限为可写）	
chmod u-x   文件或目录名        （给指定的文件或目录 取消  所有者可写权限）

设置x才可以进入目录或文件， 修改权限后先logout



			定时任务调度
crond   任务调度
crontab 指令（定时任务）进行定时任务的设置
任务调度： 指系统在某个时间执行的特定的命令或程序
任务调度分类：  1. 系统工作，有些重要的工作必须周而复始地执行，如病毒扫描等     2. 个别用户工作，个别用户可能希望执行某些程序，比如对mysql
语法
crondtab  [选项]
常用选项： -e  编辑crontab定时任务      -l  显示crontab任务       -r  删除当前用户所有的crontab任务

在  crontab -e  后 输入
如：    */1  *  *  *  *  ls >> xx/xx.txt      表示每小时的第一分钟执行一次（注意空格）
5个占位符的说明
第一个	*      一小时当中的第几分钟  （范围0-59）
第二个	*      一天当中的第几小时   （范围0-23）
第三个      *      一个月当中的第几天 （范围1-31）
第四个	*      一年当中的第几月  （范围1-12）
第五个	*     一周当中的星期几    （范围0-7， 0和7都是星期日）

*      代表任何时间。比如第一个“*”就代表一小时中每分钟都执行一次的意思.
,     代表不连续的时间。比如0 8,12,16  * * *命令.,就代表在每天8点0分. 12点0分, 16点0分都执行一次命令
-    代表连续的时间范围。比如0 5 * * 1-6命令,代表在周一到周六的凌晨5点0分执行命令
*/n     代表每隔多久执行一次。比如*/10 * * * * 命令,代表每隔10分钟执行一遍命令
注意 星期几和一个月的第几天都是来指定 天 的，最好不要两个一起用

重启任务调度
service crond restart

		
			linux磁盘分区，挂载
1024B=1KB  1024KB=1MB 1024MB=1GB 1024GB=1TB 1024TB=1PB 1024PB=1EB

1) Linux来说无论有几个分区,分给哪一目录使用,它归根结底就只有一个根目录,一个独立且,唯一的文件结构, Linux中每个分区都是用来组成整个文件系统的一部分.
2) Linux采用了一种叫“mount”的处理方法,它的整个文件系统中包含了一整套的文件和目录,且将一个分区和一个目录联系起来。这时要载入的一个分区将使它的存储空间在一个目录下获得。
	就是 Linux下一切都是文件， 分区挂载到对应的目录结构上，操作对应目录相当操作硬盘，也可以卸载umount
linux硬盘分ide和scsi硬盘，目前基本上为scsi硬盘
sda1  (sd  表示分区所在设备的类型   a为基本盘，a之外还有b基本从属盘， c辅助主盘，d辅助从属盘   数字1表示前四分区的主分区，2，3，4也是主分区或扩展分区，5开始就是逻辑分区)


lsblk -f  (list block，记 老师不离开 或 六十八离开)    查看系统的分区和挂载的情况
查看时显示,对应行的理解
NAME		FSTYPE		LABEL	UUID	MOUNTPOINT
分区情况		文件类型		标签	唯一标识	       挂载点
直接使用   lsblk  可查看分区的大小

案例：  现在需要给linux系统增加一个新的硬盘，并且挂载到/home/xx   目录		
1. 虚拟机添加硬盘
在vmware中，点击虚拟机->设置->点击磁盘->看下面有添加点击->点硬盘->next->SCSI(recommend)->next->create new virtual disk ->next->分配大小->next->next->finish-> reboot linux
2.分区, sdb为新加的磁盘, 分一个主分区sdb1
fdisk /dev/sdb    - >  m(help) ->n(add a new partition) -> p(primary partition)->enter(default)->m(help)->w(write table to disk and exit)   (不会就使用default)
3.格式化， 把sdb1主分区格式化
mkfs  -t  xfs  /dev/sdb1
4.挂载
首先创建一个目录，然后将  sdb1挂载到该目录
mount  /dev/sdb1   xx（目录）
5.设置自动挂载
vim etc/fstab     在最后一行加入     /dev/sdb1	    /home/newdisk	ext4     defaults  0 0
mount -a   自动挂载

磁盘情况查询
df  指令  输出磁盘文件系统使用情况:
df [选项] 
常用选项： -h以适当单位显示，默认“已用”一列是KB    -i 显示inode使用情况。-l 查看当前系统磁盘的使用情况

du 指令  查询指定目录的磁盘占用情况
du [选项]  /目录
常用选项： -h 带计量单位  -s指定目录占用大小汇总   -a 含文件  -c 列出明细时增加总值   -max-depth=1 子目录深度
组合  -ach

例子：  统计/home文件夹下文件个数
	ls -l  /home  |  grep  "^-"  | wc -l
             统计/home文件夹下目录个数
	ls -l  /home  |  grep  "^d"  | wc -l
             统计/home文件夹及子文件夹下文件个数
	ls -lR  /home  |  grep  "^-"  | wc -l


			网络配置
查看ip地址：  windows下 ipconfig   linux下 ifconfig
NAT：网络地址转换器， windows下原有的连接外界的ip，除此之外，还有 两个ip构成虚拟网络，用于同机器两系统的通信
修改虚拟网络编辑器：点击vMware中的编辑，点虚拟网络编辑器，选中VMnet8，改变子网ip
查看网关： 点击vMware中的编辑，点虚拟网络编辑器，选中VMnet8，NTA设置，看到 网关ip
查看网络是否联通：  ping xxxx(域名或IP地址)
指定固定的ip：    cd   /etc/sysconfig/network-scripts    改变ifcfg-ens33 文件内容， 在内容中加入对应的ip（指定不变的ip），gateway（与母机保持一致），dns（与gateway保存一致），将bootproto设为static，onboot=yes， 然后重启


				进程管理
在linux中，每个执行的程序（代码）都是一个进程，每个进程读分配一个id号。
每个进程，都会对应一个父进程，而这个父进程可以复制多个子进程， 例如www服务器
每个进程都可能以两种方式存在的，前台与后台， 所谓前台就是用户目前的屏幕上可以进行操作的，后台进程则是实际操作的，但由于屏幕上无法看到进程，通常使用后台方式执行
一般系统的服务器都是以后台进程的方式存在，而且都会常驻于系统中，直到关机才结束

ps 指令 用来查看目前系统中，有哪些正在执行的进程，以及他们的执行状况
常用选项：   -a  显示当前终端的所有进程信息     -u  以用户的格式显示进程信息    -x  显示后台进程运行的参数   -ef  查看可父进程
   	pstree指令 ： 让结构更清晰    常用选项： -p 显示进程的PID     -u  显示进程的所属用户   
ps执行后显示：
USER        		PID 		%CPU 		%MEM    		VSZ   		RSS 		TTY      		STAT 			START   	   TIME 		COMMAND
执行进程的用户	进程号		占用cpu的情况	占用内存的情况	占有的虚拟内存	使用物理的内存	使用的终端	进程的state（s休眠r运行）	启动时间       占用cpu的时间	进程执行时的命令行
使用管道指令查找：  ps -aux  | grep  xxx

终止进程
若某个进程执行了一半需要停止时，或是已销耗很大系统资源时，此时可以考虑停止该进程
使用
kill 和 killall
语法：
kill  [选项]   进程号
killall   进程名名称，支持通配符
-9    强制弄死

杀掉非法用户     kill   xxx(非法用户的进程号， 可通过 ps  -aux | sshd 查看用户)

			服务管理
service本质上就是进程，但是运行在后台的，通常都会监听某个端口，等待其他的程序的请求，比如(mysql,sshd,防火墙等)，因此我们又称为守护进程
service管理指令：
sevice（centos7.0后使用systemctl）start | stop | restart | reload |  status  服务名 

telnet  指令 检查远程的服务器的某个端口是否在监听（运行）
windows下要使用telnet， 需要先搜索telnet，点击启用或关闭Windows功能，找到telnet client勾选，确定，之后就可以用了
如果 修改service管理的状态，想永久生效， 使用 chkconfig指令 

查看系统提供的服务  ：  setup 回车 选中system service        或          /etc/init.d/服务名称

chkconfig 指令  可以给每个服务的各个运行级别设置自启动/关闭
chkconfig 服务名 --list
chkconfig  --list | grep xx
chkconfig  --level 5  服务名 on/off
chkconfig  服务名  on/off     所有运行级别on/off

systemctl  list-unit-files   查看服务
systemctl  list-dependencies  运行级别名称

动态监控进程
top 指令 与ps很像，但top执行一段时间可以更新正在执行的进程
top  [选项]
常用选项：  -d  指定刷新时间（默认为3秒1刷）
top后，看到 第一行  当前时间   运行时间   当前登录系统的用户数    负载均衡
第二行， 任务总数	正在运行的任务数	正睡任务数....
输入 u  查看指定用户的进程	输入 k  指定删除某进程	q退出
输入 P  以cpu使用率排序	输入 M  以内存使用率排序	N以PID排序

netstat 指令   查看系统网络情况
netstat [选项]
常用选项： -an 按一定顺序排列输出        -p  显示那个进程在使用
可以查看哪些端口在listen或已经建立连接等信息




			RPM包的管理
一种用于互联网下载包的打包及安装工具，它包含在某些Linux分发版中， 它生成具有.RPM扩展名的文件。
RPM是RedHat Package Manager（红帽包管理）的缩写， 类似windows的setup.exe,  这一文件格式名称虽然打上了Redhat的标志，但理念是通用的
linux的分发版本（suse，redhat，centos等）都有采用

用rpm指令查询已安装的rpm列表：    rpm qa | grep xx      (qa ： query all)
查看已安装的软件包中的文件：     rpm -ql 软件包名
查询文件所属的软件包：          rpm   -qf  文件的全路径名

firefox-68.5.0-2.el7.centos.x86_64
名称	版本     linux版本  Linux的发行版  64位系统	

卸载rpm包：  
rpm -e rpm包的名称       （加  --nodeps xx 强制删除，不推荐）
安装rpm包：
rpm  -ivh  rpm包名       （i install  v verbose h hash）

	yum 
yum是一个Shell前端软件包管理器。基于RPM包管理,能够从指定的服务器自动下载RPM包并且安装,
可以自动处理依赖性关系,并且一次安装所有依赖的软件包。
yum的基本指令
查询yum服务器是否有需要安装的软件
	yum list | grep xx软件列表·
安装指定的yum包
	yum install xx下载安装
